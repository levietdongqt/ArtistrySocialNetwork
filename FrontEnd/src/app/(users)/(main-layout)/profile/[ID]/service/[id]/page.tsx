import React from 'react';

const ServicePage = ({ /* props nếu có */ }) => {
    // Nội dung của Component
    return (
        <div>
            {/* Các element của trang service cho một ID cụ thể */}
        </div>
    );
};

export default ServicePage;